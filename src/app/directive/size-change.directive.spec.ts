import { SizeChangeDirective } from './size-change.directive';

describe('SizeChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeChangeDirective();
    expect(directive).toBeTruthy();
  });
});
